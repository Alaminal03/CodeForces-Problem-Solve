#include<bits/stdc++.h>
using namespace std;

int man()
{
    int t;
    cin >> t;
    while(t--)
    {
        int m, n;
        cin >> m >> n;

    
        for (int i = 0; i < m;i++)
        {
            string s;
            cin >> s[i];
        }
        for (int i = 0; i < n;i++)
        {
            string p;
            cin >> p[i];
        }

        for (int i = 0; i < n;i++)
        {

        }
    }

    return 0;
}